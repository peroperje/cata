apiVersion: apps/v1
kind: Deployment
metadata:
  name: cata-postgres
  labels:
    app: cata-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cata-postgres
  template:
    metadata:
      labels:
        app: cata-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine # Or use your custom built image if pushed to registry
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "postgres"
            - name: POSTGRES_DB
              value: "cata_db"
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: postgres-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: cata-postgres
spec:
  selector:
    app: cata-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
